{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --parallel dev:frontend dev:backend",
    "dev:frontend": "bun --filter frontend dev",
    "dev:backend": "bun --filter portfolio-backend dev",
    "build": "bun --filter '*' build",
    "build:ci": "bun --filter frontend build:ci",
    "check:ci": "bun --filter frontend check:ci",
    "test:unit": "bun --filter '*' test:unit",
    "lint": "bun --filter '*' lint",
    "format": "bun --filter '*' format",
    "format:check": "bun --filter '*' format:check"
  },
  "dependencies": {
    "hono": "^4.12.30"
  },
  "devDependencies": {
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.111.0"
  },
  "engines": {
    "bun": "^1.0.0"
  }
}
